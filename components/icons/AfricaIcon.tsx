import React from 'react';

// This component is intentionally left blank to resolve a rendering issue.
export const AfricaIcon: React.FC<React.SVGProps<SVGSVGElement>> = (props) => (
    <svg {...props} />
);
